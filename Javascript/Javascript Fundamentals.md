let a = 0 ;  initializing variable a with a default value of 0
const a    Declares a constant 
Math.random() gives a random decimal between 0 and 1 
Math.floor() truncates to return an integer

console.log()  is used to display output
parseInt() converts text into int 

if (A || B)
  {}
else if 
  {}
else
  {}

// Repeat until the a = b.
while (a !== b) {
    // Loop body: instructions to be repeated
}
